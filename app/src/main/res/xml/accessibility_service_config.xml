<?xml version="1.0" encoding="utf-8"?>
<!--
    Asks for as close to nothing as the API allows.

    canRetrieveWindowContent is false and there is no package filter, because there is
    nothing to filter: the service exists to call one global action. The grant screen will
    still warn the user in strong terms, because Android cannot tell a service that reads
    everything from one that reads nothing — the description string has to do that job.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canPerformGestures="false"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_description"
    android:notificationTimeout="1000" />
